# Python
__pycache__/
*.py[cod]
*$py.class
.Python
*.so
.eggs/
*.egg-info/
dist/
build/
.venv/
venv/
env/

# IDE / editor
.idea/
.vscode/
*.swp
*~

# Build logs and local scratch (offline index runs, sweeps)
logs/
build_full*.log
*.log

# Dev tuning output (regenerated by scripts/dev/tune_*.py)
scripts/dev/tune_*_results.csv

# Full Wikipedia corpus is in the course handout (not in git)
data/*
!data/public_queries.json

# Course handout copies kept locally (not part of submission)
local/

# Manual index backups (do not commit duplicate artifact trees)
artifacts_backup*/

# Chunk-size sweep experiments (copy winner into artifacts/ for submit)
artifacts_sweep/

# Intermediate build shards (final faiss/BM25/page_ids are enough for grading)
artifacts/shards/
artifacts/build_checkpoint.json

# OS
.DS_Store
Thumbs.db
